spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/test_tes
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: asqarovdanater@gmail.com
    password: kpdz vbti hkyc iawb
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none   # ??? dev ?????????, ??? prod ? validate
    show-sql: true
    properties:
      hibernate:
        default_schema: public
        format_sql: true




  sql:
    init:
      mode: never
